# 기술블로그 실무 사례

국내 기술블로그(우아한형제들, 토스, 카카오페이, 올리브영, 당근, LINE, 컬리, 화해 등)에서 다룬 **실제 장애·설계 사례**를 CS 주제별로 모았습니다. 각 노트의 개념이 실무에서 어떻게 문제로 나타나고 해결됐는지를 한 화면에서 훑어보며, 면접에서 "실제 경험/사례"를 묻는 질문에 근거로 활용하기 위한 자료입니다.

각 항목 끝의 `(출처: 회사)`는 맨 아래 **참고한 기술블로그** 목록의 원문에 대응합니다.

## Backend Core

### Java

- Kubernetes에서 `requests.cpu`를 2에서 1로 줄이자 GC/heap 설정 변경 없이 메모리 90% 초과 경보가 떴는데, 원인은 heap이 아니라 스레드 스택 메모리 증가였습니다. JVM의 `+UseContainerSupport`가 `cpu.shares == 1024`(정확히 1 core)를 "제한 없음"으로 해석하는 Hotspot 버그 때문에 `availableProcessors()`가 노드 전체 코어 수(36)를 반환했고, CPU 수 기반으로 스레드를 정하는 라이브러리가 스레드를 4개에서 36개로 폭증시켰습니다. (출처: 하이퍼커넥트)
- 이처럼 1GB 메모리 환경에서 스레드 약 20개 증가가 약 100MB 추가 메모리 소비로 이어졌고 GC 지표는 변화가 없어 heap 문제가 아님을 알 수 있었습니다. 해결책은 `limits.cpu` 명시, `requests.cpu`를 1024의 배수가 아닌 값(999m, 1001m)으로 설정, `-XX:ActiveProcessorCount` 명시 중 선택이며 해당 버그는 JDK 19에서 수정(JDK-8281571)되었습니다. (출처: 하이퍼커넥트)
- RestTemplate 호출 시 URL에 가변 ID를 문자열로 직접 연결(`"/find/" + id`)하면, 메트릭 수집 라이브러리(Netflix Servo)가 각 URL을 고유 키로 수집해 메모리가 무한 누적되는 누수가 발생했고 heap dump 분석 결과 `BasicTimer`가 heap의 70.97%를 점유했습니다. Full GC가 빈발하는데 heap이 회수되지 않는 패턴이 memory leak의 전형적 신호이며, 코드만 보고 찾기보다 heap dump(Eclipse MAT) 분석이 필수입니다. (출처: 우아한형제들)
- 해결은 URL 변수 바인딩 방식(`getForObject(url, Class, id)`)으로 변경하고 문제 라이브러리를 비활성화하는 것으로, 수정 전 부하 테스트에서 5분 만에 TPS가 급락했으나 수정 후 297 가상 유저로 10시간 안정화되었습니다. 3rd-party 라이브러리의 기본 활성 기능이 예기치 않은 누수를 유발할 수 있고, 트래픽 증가에 따라 잠복하다 발현되는 유형은 사전 부하 테스트로 예방할 수 있습니다. (출처: 우아한형제들)
- JDBC 드라이버를 MariaDB에서 MySQL(mysql-connector-j)로 교체한 뒤 주기적 OOM이 발생했는데, Old Gen(2GB) 포화 상태에서도 Full GC가 회수하지 못하고 Minor GC만 반복되었고 `jmap` 덤프를 Eclipse MAT로 분석하니 `AbandonedConnectionCleanupThread`가 힙 대부분을 점유하며 `ConnectionImpl` 인스턴스 13,689개가 적체돼 있었습니다. 근본 원인은 이 스레드가 PhantomReference를 단일 스레드로 하나씩 꺼내 소켓을 정리하는 구조인데 HikariCP `max-lifetime`을 기본값(30분)의 1/36인 50초로 줄여 Connection 재생성 속도가 정리 속도를 초과한 것으로, 해결은 mysql-connector-j 8.0.22 이상과 `-Dcom.mysql.cj.disableAbandonedConnectionCleanup=true`입니다. (출처: 컬리)
- WebClient 장애 시점에는 G1 Eden Space 급감, GC 빈발, CPU 스파이크가 관측됐는데 이는 GC가 반복돼도 새 객체 할당용 메모리가 확보되지 않는 OOM 직전의 전형적 패턴입니다. 원인은 APM 에이전트(OpenTelemetry 1.9.1)가 커넥션 풀 키 계산 객체에 매 요청마다 달라지는 값을 주입해 풀이 재사용되지 않고 `ConcurrentHashMap<PoolKey, InstrumentedPool>`에 항목이 수만 개 누적된 것이었습니다. (출처: 하이퍼커넥트)
- heap 분석은 Shallow Heap이 아니라 Retained Heap(해당 오브젝트를 GC 했을 때 회수 가능한 바이트 수) 기준으로 봐야 문제 객체를 찾기 쉬우며, 이 사례의 문제 ConcurrentHashMap은 Shallow가 1.4MB에 불과했지만 Retained는 180MB였습니다. 로컬에서 재현되지 않는 누수는 에이전트·베이스 이미지 등 환경 차이에서 비롯될 수 있고, Kubernetes Liveness Probe는 장애 자동 복구에는 유효하지만 근본 원인 해결과는 무관합니다. (출처: 하이퍼커넥트)
- `+` 연산자나 String Template로 문자열을 합치면 변수의 문자열 변환(`String.valueOf()`/`toString()`)과 concatenation의 두 단계 연산이 발생하며, JDK 버전에 따라 StringBuilder 또는 StringConcatFactory(Java 9+, invokedynamic 기반)로 최적화됩니다. (출처: 카카오페이)
- 로깅에서는 DEBUG 레벨이 비활성화되어 있어도 로그 문자열 연산이 먼저 실행되는 함정이 있어, SLF4J parameterized logging(`logger.debug("... {}", t)`)은 레벨이 활성화됐을 때만 치환이 일어나 이를 방지하며 SLF4J 공식 문서 기준 `+` 방식 대비 최소 30배 우위입니다. (출처: 카카오페이)
- 10만 건 DEBUG 로그를 DEBUG 비활성화 상태로 측정한 벤치마크에서 String Template는 96.5ms, Parameterized Logging은 2.5ms, kotlin-logging 람다는 15.5ms였습니다. parameterized logging은 인자 3개 이상일 때 `Object[]` 생성 비용과 가독성 저하가 있어, 성능과 가독성의 균형점으로 kotlin-logging 람다 방식을 고려할 수 있습니다. (출처: 카카오페이)

### Spring

- `@Bean` 메서드 이름이 그대로 bean name으로 등록되며, 동일 타입 빈이 1개면 이름과 무관하게 타입 기반으로 주입됩니다. (출처: 카카오페이)
- 동일 타입 빈이 여러 개면 `@Qualifier`(bean name 강제 매칭, 실패 시 즉시 오류) > `@Primary`(타입 기반, field name 무시) > `@Priority` > field name ↔ bean name 매칭 순으로 후보가 결정됩니다. (출처: 카카오페이)
- `@Primary` 빈이 존재하면 field name 기반 매칭이 무력화되어 의도치 않은 빈이 주입될 수 있습니다. `@Qualifier`는 후보 탐색 이전 단계에서 처리되므로 `@Primary`보다 우선합니다. (출처: 카카오페이)
- field injection은 빈이 없을 때 런타임에야 NPE로 드러나는 반면, 생성자 주입은 `final` 필드로 컨텍스트 로딩 시점에 오류를 감지할 수 있어 권장됩니다. (출처: 카카오페이)
- Spring AOP는 JDK Dynamic Proxy든 CGLIB든 `this`를 통한 내부 함수 호출은 프록시를 거치지 않아 부가기능이 동작하지 않으며, private/protected 메서드에도 프록시 기반 AOP는 적용되지 않습니다. (출처: 카카오페이)
- Pointcut 표현식은 컴파일 시점에 검증되지 않아, 패키지·클래스명을 변경하면 AOP가 조용히 빠지고 런타임에야 발견됩니다. (출처: 카카오페이)
- `joinPoint.args`가 타입 정보 없는 배열로 반환되어 인자명·순서 변경 시 런타임 오류가 나고, `@Cacheable`의 SpEL key도 인자명 불일치 시 잘못된 캐시 키를 만들지만 컴파일에서 잡히지 않습니다. (출처: 카카오페이)
- 내부 호출 우회를 위해 클래스를 분리하면 서비스 레이어 계층이 늘어나 복잡도가 증가하는 트레이드오프가 있습니다. (출처: 카카오페이)
- Access log에는 실제 요청 URL(`/v1/orders/A0000001`)만 남아 컨트롤러의 URL 패턴(`/v1/orders/{id}`)과 매핑하기 어렵습니다. 이는 다수 API 중 미사용 API를 찾을 때의 출발점 문제입니다. (출처: 우아한형제들)
- `RequestMappingHandlerMapping`은 매칭된 URL 패턴을 `HandlerMapping.BEST_MATCHING_PATTERN_ATTRIBUTE` 속성으로 `HttpServletRequest`에 저장하므로, Interceptor의 `preHandle()`에서 이 속성을 읽으면 패턴 단위 로그를 남길 수 있습니다. 핸들러 정보를 활용하는 로깅은 Filter보다 Interceptor가 유리합니다. (출처: 우아한형제들)
- `RequestMappingHandlerMapping.getHandlerMethods()`로 등록된 전체 (HTTP method, URL 패턴) 목록을 얻고, 수집한 호출 카운트가 0인 항목을 미사용 API로 판별할 수 있습니다. (출처: 우아한형제들)
- `IllegalArgumentException`은 클라이언트 잘못뿐 아니라 프레임워크/라이브러리 내부 버그(예: `Thread.setPriority()`)로도 발생하므로, 일괄 400으로 매핑하면 서버 결함이 클라이언트 잘못으로 오인되어 대응이 지연됩니다. Spring 기본 동작도 이 예외를 500으로 처리합니다. (출처: 우아한형제들)
- 4xx/5xx 판단 기준은 오류 책임이 클라이언트냐 서버냐입니다. 4xx는 요청 수정 없이는 재시도가 무의미한 경우, 5xx는 일시 장애일 수 있어 backoff 재시도가 가능한 경우입니다. (출처: 우아한형제들)
- 상태 코드를 혼용하면 모니터링이 망가집니다. 서버 오류를 4xx로 내리면 신속 대응이 어렵고, 클라이언트 오류를 5xx로 올리면 경보 피로도가 증가합니다. (출처: 우아한형제들)
- 범용 예외를 직접 400에 매핑하지 말고 `code`를 가진 커스텀 비즈니스 예외 계층을 정의한 뒤, `@RestControllerAdvice` + `@ExceptionHandler`로 의도된 클라이언트 오류만 400으로 매핑하고 예상치 못한 예외는 기본 500 동작에 맡기는 전략이 권장됩니다. (출처: 우아한형제들)
- 예외가 프록시를 통과하면 `TransactionInterceptor`가 `rollbackOn`을 확인해 unchecked exception이면 트랜잭션에 rollback-only를 마킹하며, 이 마킹은 트랜잭션 단위이므로 호출자가 예외를 catch해도 커밋 시점에 롤백됩니다. (출처: 하이퍼커넥트)
- 반대로 프록시를 거치지 않은 메서드에서 발생한 예외를 catch하면 마킹이 없어 정상 커밋되고, self-invocation에서는 `REQUIRES_NEW`를 선언해도 새 트랜잭션이 열리지 않습니다. (출처: 하이퍼커넥트)
- Kotlin은 checked exception 선언 강제가 없어 checked exception이 프록시를 통과하면 `UndeclaredThrowableException`(RuntimeException 계열)으로 래핑되어 의도와 달리 롤백되므로, `@Throws`를 명시해야 checked exception이 그대로 전달됩니다. (출처: 하이퍼커넥트)
- `REQUIRES_NEW`는 동일 스레드에서 수행되는 새 물리 트랜잭션이라, 이미 커밋된 자식은 부모가 롤백돼도 되돌아가지 않고, ThreadLocal 기반이라 `@Async` 등 다른 스레드에는 전파되지 않습니다. (출처: 하이퍼커넥트)
- 긴 트랜잭션을 `REQUIRES_NEW` + 커서 기반 배치로 분할하면 재시도 시 이미 성공한 분량을 재처리하지 않으면서 트랜잭션 점유 시간을 줄일 수 있습니다. (출처: 하이퍼커넥트)

### 데이터베이스

- 트랜잭션 선언 자체에도 비용이 있습니다. `@Transactional(readOnly=true)`만 붙여도 본 SELECT 외에 `SET autocommit=0/1`, `SET SESSION TRANSACTION ...`, `COMMIT` 등 최대 6개의 부가 쿼리가 발생해 네트워크 왕복이 최대 6배가 됩니다. 실측에서는 peak 기준 `set_option` 계열 쿼리가 약 14K QPS로 실제 SELECT/COMMIT(약 5K QPS)보다 많았고, 불필요한 트랜잭션을 제거하자 SELECT 처리량이 약 2~3배, API 응답 성능이 약 2배 개선되었습니다. (출처: 카카오페이)
- 트랜잭션 경계 기준: 단순 조회와 단건 UPDATE/INSERT는 DB 자체 원자성으로 충분해 트랜잭션이 불필요하고, 여러 변경을 원자적으로 묶어야 할 때만 `@Transactional`을 사용합니다. (출처: 카카오페이)
- 읽기 전용 조회는 `@Transactional(readOnly=true, propagation=SUPPORTS)`로 단독 호출 시에는 트랜잭션 없이 동작하되 상위 트랜잭션이 있으면 참여하게 할 수 있습니다. NEVER는 상위 트랜잭션 존재 시 예외가 발생하고, NOT_SUPPORTED는 추가 커넥션 점유 위험이 있어 배제되었습니다. (출처: 카카오페이)
- 트랜잭션 사용 구간 안에 3rd party API 호출이 끼지 않도록 경계를 잡는 것이 원칙입니다. 외부 I/O가 트랜잭션 안에 있으면 커넥션과 lock 점유 시간이 길어집니다. (출처: 카카오페이)
- REPEATABLE READ의 consistent nonlocking read는 트랜잭션 내 첫 번째 읽기 시점의 snapshot을 계속 사용하고, COMMIT이 실행되어야 다음 쿼리가 새 snapshot을 얻습니다. `auto-commit=false` 환경에서 COMMIT 없이 커넥션을 재사용하면 과거 snapshot을 계속 읽게 되어, Master DB에 INSERT된 데이터가 같은 Master DB의 다른 세션 SELECT에서 간헐적으로 조회되지 않는 장애로 이어질 수 있습니다. (출처: 컬리)
- OSIV(`open-in-view=true`)는 커넥션을 API 종료 시점까지 유지하고, `@Transactional`이 없는 조회 메서드는 COMMIT을 발생시키지 않아 snapshot 갱신 시점이 없어집니다. 조회 메서드에 `@Transactional(readOnly=true)`를 붙여 메서드 종료 시 COMMIT되게 하면 매 요청마다 snapshot이 갱신되며, 부가 효과로 `SET autocommit=0/1` 쿼리가 제거되어 API 응답 속도가 약 40%(1.5ms) 개선되었습니다. 격리 수준을 낮추는 대안은 phantom read 등 기존 로직에 대한 사이드 이펙트 우려로 채택하지 않았습니다. (출처: 컬리)
- MVCC 구현은 DB마다 다릅니다. Oracle/MySQL은 이전 버전을 별도 UNDO segment에 보관하고 자동 회수하지만, PostgreSQL은 같은 데이터 페이지 안에 구버전과 신버전 tuple을 함께 저장하고 tuple마다 `xmin`/`xmax`를 기록합니다. UPDATE 시 기존 tuple이 dead tuple로 남아 공간 낭비와 성능 저하를 유발하며 Vacuum이 GC 역할을 합니다. 일반 Vacuum은 OS에 디스크 공간을 반환하지 않고, 반환하려면 Access Exclusive Lock을 잡는 `VACUUM FULL`이 필요합니다. (출처: 우아한형제들)
- 오래 열린 트랜잭션(long/idle transaction)이 있으면 그 시점 이후의 dead tuple을 Vacuum이 정리하지 못하므로 `statement_timeout`, `idle_in_transaction_session_timeout`으로 방치 트랜잭션을 제어해야 합니다. 미사용 replication slot, `hot_standby_feedback=ON`도 dead tuple 정리를 막습니다. (출처: 우아한형제들)
- PostgreSQL Transaction ID는 4바이트(약 40억)로 소진 후 재순환되면 기존 데이터가 미래 데이터로 인식되어 손실되는 Wraparound가 발생할 수 있고, 오래된 tuple의 ID를 frozen XID로 바꾸는 Freeze로 방지합니다. `autovacuum_freeze_max_age`(기본 2억)를 초과하면 AutoVacuum OFF 설정도 무시하고 강제 실행됩니다. (출처: 우아한형제들)
- Gap Lock은 내부적으로 Shared 성질만 가져 서로 다른 트랜잭션의 Gap Lock끼리는 충돌 없이 동시 획득이 가능하지만, INSERT가 요구하는 Insert Intention Gap Lock은 기존 Gap Lock과 호환되지 않습니다. 그래서 존재하지 않는 id에 두 트랜잭션이 각각 `SELECT ... FOR UPDATE`/`DELETE`를 실행하면 둘 다 즉시 성공하지만, 이후 양쪽이 같은 id를 INSERT하려는 순간 서로의 Gap Lock 해제를 상호 대기하며 데드락이 발생합니다. (출처: 당근)
- 인덱스 종류별 락 범위가 다릅니다. PK/Unique Index로 정확히 1건이 특정되면 Record Lock만 사용하고, 대상 레코드가 없거나 복합 유니크 인덱스의 일부 컬럼만 조건에 쓰이면 Gap Lock이 추가되며, non-unique secondary index는 결과 건수와 무관하게 항상 Record Lock + Gap Lock을 사용합니다. (출처: 당근)
- 테이블 레코드가 적을수록 간격이 넓어 잠금 범위가 커집니다(빈 테이블이면 Infimum~Supremum 전체가 잠김). 락 상태는 `performance_schema.data_locks`에서 `LOCK_MODE: X,GAP`으로 확인할 수 있습니다. (출처: 당근)
- Gap Lock의 목적은 REPEATABLE READ 격리 수준 보장, STATEMENT/MIXED binlog 포맷의 replication 일관성 보장, Foreign Key 일관성 보장 3가지입니다. `binlog_format=ROW` + `transaction_isolation=READ-COMMITTED`로 최소화할 수 있지만, 격리 수준 변경은 금전 관련 서비스에서 매우 신중해야 하며 데드락이 사라지는 대신 서로 다른 트랜잭션의 INSERT 레코드가 섞여 저장되는 의도치 않은 버그가 생길 수 있습니다. (출처: 당근)
- 재고 확인, 결제, 재고 차감, 배송 처리, 주문 완료 생성이 하나의 큰 트랜잭션으로 묶이면, 동시 주문 시 한 트랜잭션이 상품 A의 lock을 잡은 채 상품 B를 기다리고 다른 트랜잭션은 반대 순서로 대기하는 데드락이 발생할 수 있습니다. 데드락이 누적되면 lock 대기로 시스템 리소스가 고갈되고 최악의 경우 시스템 셧다운까지 이어질 수 있습니다. (출처: 올리브영)
- 하나의 거대 트랜잭션을 상품 단위의 개별 트랜잭션으로 분리해 각각 즉시 commit되게 하면 트랜잭션 범위 축소가 곧 lock 점유 시간 축소로 이어집니다. JMeter 부하 테스트에서 개선 후 데드락이 발생하지 않았고, 실제 운영 트래픽에서도 안정적으로 처리됨을 확인했습니다. (출처: 올리브영)
- 트랜잭션을 쪼개면 원자성을 애플리케이션 차원에서 보상해야 하는 trade-off가 있습니다. 분리 시 1+1 상품·증정품·예약상품 등 속성별 처리 로직과 실패 시 롤백(보상) 경로까지 함께 설계해야 합니다. (출처: 올리브영)

### Redis

- 시간순 정렬이 필요한 피드는 Sorted Set의 score에 timestamp를 넣어 구현할 수 있습니다. (출처: 올리브영)
- 객체를 직렬화해 String으로 저장하면 필드 추가/삭제 시 역직렬화 오류가 나기 쉬우므로, 필드 변경이 잦은 객체는 Hash 타입이 유연합니다. (출처: 올리브영)
- Big Key 위험 기준을 수치로 잡을 수 있습니다: 문자열 1MB 초과, 컬렉션 요소 10,000개 초과. 부작용으로 메모리 단편화, 성능 저하, 네트워크 포화, 복제 동기화 지연, 클러스터 데이터 불균형이 생깁니다. (출처: 올리브영)
- 글로벌 캐시(Redis) 앞에 로컬 캐시를 계층화하면 Redis 호출 자체를 줄일 수 있습니다. 단 로컬 캐시 단독 사용은 분산 환경 일관성 문제를 일으키므로, 항상 글로벌 캐시(버전 조회)와 조합해야 합니다. (출처: 올리브영)
- 특별한 이유가 없는 한 모든 key에 TTL을 설정하는 것이 권장됩니다. 근거는 메모리 고갈(OOM) 방지, 데이터 최신성, 캐시 정합성, 장애 시 자동 정리입니다. (출처: 올리브영)
- 핫키의 TTL이 만료되면 요청이 원본 DB로 몰리는 stampede가 생길 수 있으므로, 백그라운드 주기 갱신이나 PER 알고리즘 적용이 권장됩니다. (출처: 올리브영)
- avalanche(캐시 쇄도)에는 TTL jitter가 유효합니다. 만료 시간에 0~10초 같은 무작위 지연을 더해 DB 부하를 시간축으로 분산하되, jitter가 길수록 사용자가 오래된 데이터를 볼 수 있으므로 서비스별 최대 jitter 상한을 정해야 합니다. (출처: 토스)
- penetration(캐시 관통)에는 null 오브젝트 패턴을 씁니다. DB 조회 결과가 null이어도 캐싱하고, 원시 타입은 "값 없음"을 나타내는 sentinel 값(예: 양수 데이터라면 정수 최솟값)을 사용합니다. 대안인 bloom filter는 정합성이 깨지면 전체 캐시 재적재가 필요해 운영 복잡도가 높습니다. (출처: 토스)
- 캐시 시스템 자체 장애에는 기능 중요도 기반 failover를 설계합니다. 핵심 기능만 DB fallback을 허용하고 부가 기능은 일시 중단하거나 대체 UI를 제공합니다. 공통화된 캐시 코드는 중요도 구분 없이 전부 DB로 fallback하기 쉬우므로 사전 설계가 필요합니다. (출처: 토스)
- 핫키 만료로 인한 stampede 대응으로 분산 락을 쓰면 캐시 미스 시 락을 잡은 요청 1개만 DB 조회·캐싱을 수행합니다. 대안인 TTL 제거/백그라운드 갱신은 더 이상 핫키가 아닌 데이터가 캐시 공간을 계속 차지하는 낭비가 생길 수 있습니다. (출처: 토스)
- PER(Probabilistic Early Recomputation)은 캐시 만료 전에 확률적으로 미리 재계산하는 스탬피드 방지 기법입니다. 현재 시각이 `expiry - β × δ × ln(rand())` 조건을 만족하면 조기 재계산하며, 재계산 시간(δ)이 길수록·남은 TTL이 짧을수록 갱신 확률이 높아집니다. 구현 시 캐시 데이터와 함께 재계산 소요 시간(δ)을 Redis에 같이 저장해야 합니다. (출처: 화해)
- PER은 락 기반 대응과 달리 락 대기·락 장애 지점이 없고 구현이 단순하지만, β(공격성 조절 파라미터, 기본 1.0)가 클수록 불필요한 조기 재계산이 늘어납니다. (출처: 화해)
- 메모리 한계 도달 시 증상은 swap 사용 여부에 따라 갈립니다. swap을 쓰면 죽지는 않지만 디스크 I/O로 응답 속도가 급격히 저하되고, swap이 없으면 `OOM command not allowed when used memory > 'maxmemory'` 에러로 쓰기가 거부됩니다. (출처: 에스코어)
- `maxmemory`(메모리 상한)와 `maxmemory-policy`(초과 시 제거 규칙)를 함께 설정하는 것이 메모리 운영의 핵심입니다. (출처: 에스코어)
- eviction 정책 8종은 대상 범위(전체 key vs TTL 설정 key)와 기준(LRU/LFU/random/TTL 임박)의 조합으로 정리할 수 있습니다. 정책 선택은 순수 캐시인지, 유실 불가 데이터가 섞여 있는지 같은 서비스 특성에 따라 달라집니다. (출처: 에스코어)
- hot key로 인한 네트워크 병목에는 DB → 글로벌 캐시(Redis) → 로컬 캐시의 다중 레이어 캐시 구성이 유효합니다. 로컬 캐시로 Redis 호출 자체를 줄여 병목을 해소합니다. (출처: 올리브영)
- 트래픽 증가 시 Redis의 송신 네트워크 바이트(Network Bytes out)가 지속 상승하면 대역폭 포화·장애 위험이 됩니다. 로컬 캐시 도입 후 동일 자원 기준 TPS 478% 증가, Redis Network Bytes out 99.1% 감소 사례가 있습니다. (출처: 올리브영)
- 로컬 캐시의 서버 간 데이터 불일치는 버전 키 전략으로 보완합니다. 데이터 변경 시 버전 번호를 올려 새 키(v1, v2, ...)로 캐시를 만들고, 조회 시 Redis에서 최신 버전 번호만 먼저 확인한 뒤 해당 버전의 로컬 캐시 → 없으면 Redis 순으로 조회합니다. (출처: 올리브영)

### Kafka

- 하나의 topic을 여러 consumer가 각기 다른 목적으로 독립 소비할 수 있다는 점이 "소비해도 삭제되지 않는 로그" 모델의 실무적 의미입니다. (출처: LINE)
- 단일 클러스터에 일 2,500억 건 이상 / 210TB의 레코드가 유입되고 순간 최대 초당 4GB 이상을 처리하며 50개 이상 서비스가 공유하는 규모로 운영한 사례가 있습니다. 여러 서비스가 클러스터를 공유하는 멀티테넌시 환경에서는 특정 클라이언트의 과도한 요청으로부터 클러스터를 보호하기 위해 request quota 기능을 사용합니다. (출처: LINE)
- 이런 높은 처리량의 기반은 OS 페이지 캐시 의존과 클라이언트 배칭을 통한 요청 수 최소화입니다. 로그 모델이 순차 I/O와 배치에 유리하다는 설명의 실측 근거로 쓸 수 있습니다. (출처: LINE)
- 서비스용 토픽과 분석용 토픽을 분리 운영해 처리량·리소스 요구가 다른 워크로드의 장애 영향 범위를 나눌 수 있습니다. 분석 이벤트는 S3 Sink Connector로 영구 저장해 비즈니스 DB 부하를 분리합니다. (출처: 우아한형제들)
- 같은 key → 같은 partition → 하나의 consumer가 소비하는 경로로 이벤트 순서를 보장합니다. 주문/배달 식별자를 파티션 키로 사용한 실무 사례가 있습니다. (출처: 우아한형제들)
- Transactional Outbox + CDC(Debezium MySQL source connector) 조합에서는 커넥터 태스크가 1개로 강제되어 순서는 보장되지만 처리 지연 위험이 생깁니다. 완화책으로 outbox 테이블을 토픽별로 분리하고 식별자 기반으로 N개 샤딩하여 커넥터를 분산할 수 있습니다. (출처: 우아한형제들)
- 브로드캐스트(설정 전파) 용도 토픽은 파티션 1개로 운영하고 각 서버 인스턴스가 서로 다른 consumer group id를 갖게 하면 모든 인스턴스가 동일 이벤트를 수신합니다. consumer group별 offset 독립 관리를 응용한 패턴입니다. (출처: 우아한형제들)
- `acks=0`은 응답 미확인으로 손실 가능, `acks=1`은 follower 복제 전 leader 장애 시 손실 가능(Kafka 2.8까지 기본값), `acks=all`은 ISR 복제 확인 후 성공으로 가장 강한 보장(Kafka 3.0부터 기본값)이지만 응답 대기로 느려집니다. (출처: 올리브영)
- broker 3대 기준 `replication.factor=3` + `min.insync.replicas=2`("최소 ISR은 RF-1 이하" 가이드)로 설정하면, 관리형 Kafka의 월 1회 보안패치(약 4시간) 같은 브로커 순차 재부팅 중에도 유실을 방지할 수 있습니다. (출처: 올리브영)
- broker가 저장에 성공했지만 ack이 producer에 도달하지 못하면 retry로 중복 기록이 발생합니다. `enable.idempotence=true` + `acks=all` + `max.in.flight.requests.per.connection <= 5` + `retries >= 1` 조합이면 broker가 Producer ID와 시퀀스 번호를 확인해 한 번만 저장합니다. Kafka 3.0부터 idempotence와 `acks=all`이 기본값이므로 2.x 사용 시 명시 설정이 필요합니다. (출처: 올리브영)
- producer 설정만으로 끝나지 않고 consumer 측 대응도 함께 봐야 합니다. 재기동 유실은 `auto.offset.reset=earliest` + graceful shutdown으로, ack 미전달 중복은 수동 커밋(MANUAL_IMMEDIATE) + graceful shutdown으로 대응한 사례가 있습니다. (출처: 올리브영)
- 수동 ack 모드에서 처리 로직이 `max.poll.interval.ms`(10분)를 초과하면 "Offset commit cannot be completed since the consumer is not part of an active group" 오류와 함께 rebalancing이 발생하고 중복 consume으로 이어진 장애 사례가 있습니다. `session.timeout.ms`, `heartbeat.interval.ms`, `max.poll.interval.ms` 조정으로 완화합니다. (출처: 올리브영)
- 파티션 3개에 consumer를 2대에서 3대로 증설해 파티션과 1:1로 매칭한 사례가 있습니다. 파티션 수보다 consumer가 많으면 초과분은 유휴 상태가 되고, 한 번 늘린 파티션 수는 줄일 수 없습니다(줄이려 하면 InvalidPartitionsException). (출처: 카카오페이)
- 같은 건의 중복 실행 방지를 2중으로 설계할 수 있습니다. consumer가 소비 시 DB 상태가 대상 상태가 아니면 skip하는 상태 체크(멱등 처리)와, producer 측에서 발행 전 상태를 선변경해 다음 스케줄 주기에 같은 건이 재조회되지 않게 차단하는 방식입니다. (출처: 카카오페이)
- consumer 2대가 같은 사용자 건을 동시에 처리하는 경쟁 상황을 유저 단위 락으로 직렬화하면 락 충돌 건이 실패 처리되는 트레이드오프가 있습니다. record key에 userId를 지정해 같은 사용자 건이 같은 파티션 → 같은 consumer에서 순차 처리되도록 하면 충돌 자체를 제거할 수 있습니다. (출처: 카카오페이)
- consumer 2→3대, `max.poll.records` 1→20, 처리 스레드 1→10 조정으로 분당 91건 → 728건, 전체 실행 시간 68분 → 8분으로 개선한 수치가 있습니다. 배치 크기와 스레드 수는 목표 실행 시간과 downstream 부담을 함께 고려해 결정합니다. (출처: 카카오페이)
- 메시지 처리 누락 시 offset을 되돌려 재처리하는 운영 작업이 필요한데, Kafka CLI의 `--reset-offsets`와 Admin API의 `alterConsumerGroupOffsets` 모두 consumer group이 Empty/Dead(비활성) 상태일 때만 동작합니다. 활성 상태에서 시도하면 "the group must be empty" 오류가 발생하므로 컨슈머 중지가 필수이고, 특정 파티션만 재처리하면 되는 경우에도 그룹 전체 소비가 중단되는 가용성 트레이드오프가 있습니다. (출처: 컬리)
- 무중단 대안으로 Spring Kafka의 `ConsumerSeekAware`/`ConsumerSeekCallback`(`seek()`, `seekToBeginning()`, `seekToEnd()`, `seekToTimestamp()`)을 사용하면 컨슈머 자신이 다음 poll 전에 offset 이동 요청을 처리하여 그룹을 유지한 채 재처리할 수 있습니다. (출처: 컬리)
- seek은 개별 인스턴스 수준에서만 동작하므로, 분산 환경에서는 HTTP API로 이동 요청을 받아 Redis Pub/Sub으로 전 인스턴스에 전파해 각자 offset을 이동시키는 구조로 확장한 사례가 있습니다. 구독하는 토픽이 타 조직 소유 클러스터라면 offset 재설정 권한 문제까지 겹치므로, 컨슈머 애플리케이션 스스로 offset을 제어하는 능력이 운영상 중요합니다. (출처: 컬리)

## Web & Network

### 네트워크

- 금융기관 전문(고정 길이 필드) 통신에서 TCP는 byte stream이라 메시지 경계를 보장하지 않아 패킷이 잘려 도착하는 문제가 발생합니다. length field 기반 프레이밍(수신 버퍼 누적 데이터가 전문 길이보다 작으면 버퍼를 비우지 않고 대기)으로 해결한 사례로, "메시지 경계는 애플리케이션 프로토콜이 정의해야 한다"의 실사례입니다. (출처: 당근)
- 비동기 연동에서는 커넥션 풀로 세션 수를 고정 유지하며, 상대 기관이 허용하는 세션 수와 일치시키지 않으면 강제 끊김이 발생합니다. 유휴 연결 유지를 위해 소켓 옵션 `SO_KEEPALIVE`를 활성화합니다. (출처: 당근)
- 응답 대기는 read timeout으로 제한하고, 분산 환경에서 응답 이벤트가 유실되면 타임아웃과 동일하게 처리한 뒤 별도 처리결과조회로 원거래의 성공/실패를 확인합니다. 응답 유실 시 결과를 단정하지 않는 패턴입니다. (출처: 당근)
- 관리형 Redis(AWS ElastiCache)의 idle timeout이 100초인 환경에서 클라이언트 커넥션 풀의 idle 커넥션 정리(eviction)가 비활성 상태(`timeBetweenEvictionRuns` 기본값 -1)면, 유휴 커넥션이 서버 측에서 일방적으로 끊기고 클라이언트 워치독이 재연결을 반복해 New Connection 지표가 비정상 증가합니다. 해법은 idle eviction 주기·기준을 30초로 설정해 서버 측 idle timeout(100초)보다 짧게 잡는 것 — "중간 장비/서버의 idle timeout보다 클라이언트의 유휴 관리 주기를 짧게"라는 원칙의 실사례입니다. (출처: 우아한형제들)
- 커넥션 풀 기본 전략이 LIFO라서 트래픽 피크 이후 대부분의 커넥션이 idle로 방치될 수 있습니다. FIFO로 바꾸면 커넥션을 고르게 순환시켜 idle 자체를 줄일 수 있지만 커넥션 수는 유지되는 트레이드오프가 있습니다. (출처: 우아한형제들)
- 클라이언트 네트워크가 강제로 끊기면 서버는 연결 종료를 인지하지 못하는 half-open 상태가 됩니다. 서버의 푸시 "성공"은 IO 버퍼 write 성공일 뿐 실제 수신 보장이 아니므로, 양방향 ping-pong(heartbeat)과 애플리케이션 레벨 ACK 확인이 필요합니다. (출처: LINE)
- WebSocket 대량 동시 끊김의 근본 원인이 L7 LB(HAProxy)의 `hard-stop-after` 30분 설정이었던 사례가 있습니다. LB를 공유하는 다른 서비스 배포로 LB 프로세스가 재시작되면 30분 뒤 기존 장수(long-lived) 연결이 일괄 종료됐고, 최종적으로 L4 LB로 전환해 해결했습니다. 대량 연결 끊김은 재연결 폭주 → API 부하 → DB 병목으로 장애가 전파되는 2차 피해를 만듭니다. (출처: LINE)
- 장수 연결(WebSocket)을 프록시할 때 Ingress Nginx는 `proxy-read-timeout`/`proxy-send-timeout`을 3600초 이상으로 권고합니다. 또한 프록시 노드의 conntrack 테이블이 임계치에 도달하면 패킷 드롭이 발생합니다. (출처: LINE)
- 이벤트 트래픽 중 약 1분간 14,000건 요청 중 140건 에러가 발생한 장애의 원인은 DB 커넥터 라이브러리(mariadb-java-client 1.4.6)가 새 커넥션 생성 시마다 자기 hostname을 DNS로 조회했고, DNS 상태 불량으로 lookup에 약 6초가 걸린 것이었습니다. (출처: 우아한형제들)
- DB 서버의 connect_timeout이 5초였기 때문에 "DNS 지연(6초) > 연결 타임아웃(5초)" 구조로 커넥션 수립이 실패했습니다. DNS 지연이 DB 연결 실패로 위장되어 나타나는 장애 패턴입니다. 해결책은 커넥터 업그레이드(hostname 조회가 제거된 1.5.4+, 근본 해결), hostname을 localhost로 설정, `/etc/hosts`에 IP-hostname 정적 등록(DNS 경유 제거)의 3가지였습니다. (출처: 우아한형제들)
- 타임아웃 에러의 원인이 대상 서버가 아니라 이름 해석 단계에 있을 수 있으므로, 연결 경로의 어느 구간이 지연됐는지 나눠서 봐야 합니다. (출처: 우아한형제들)
- 원격 호출 결과를 성공/실패의 2분류가 아니라 성공/실패/알 수 없음(Unknown)의 3분류로 다뤄야 합니다. 타임아웃은 "요청이 서버에서 처리됐는지 알 수 없는" Unknown 상태이므로 즉시 실패로 간주하면 안 되며, 결제 등 side effect가 있는 요청에서 특히 위험합니다. (출처: 카카오페이)
- Unknown 상태의 후처리 전략으로 즉시 재시도, 일정 시간 후 재시도, 결과 조회 후 재시도, 보상 트랜잭션(취소 요청), 수기 처리를 상황에 맞게 선택합니다. (출처: 카카오페이)
- 안전한 재시도의 전제는 멱등성 API입니다. 요청에 유니크 키(멱등키)를 포함해 동일 요청이 여러 번 도착해도 같은 응답을 반환하게 설계하면, "서버에서 처리됐지만 응답만 유실된 경우"를 멱등키로 흡수할 수 있습니다. (출처: 카카오페이)
- 예외 흐름을 try-catch 중첩 대신 Success/Failure/Unknown을 타입으로 표현해 분기하는 구현 패턴이 있습니다. (출처: 카카오페이)

### 웹 & HTTP

- 응답은 상태 줄(프로토콜 버전 + 상태 코드, 예 `HTTP/2 404`), 응답 헤더(Key:Value), 응답 본문(JSON) 3요소로 나눠 읽고, 상태 코드 5개 범주(1xx~5xx)를 기준으로 요청·에러 처리 로직을 분기합니다. (출처: 토스페이먼츠)
- 상태 코드만으로는 어떤 리소스가 왜 문제인지 알 수 없으므로, 실패 시 응답 본문에 `code`·`message` 필드로 구체적 원인을 내려주는 설계가 실무 관례입니다(예: 404 + `"code":"NOT_FOUND_PAYMENT"`). 상태 코드 범주 + 본문 에러 코드의 2단 구조로 이해하면 좋습니다. (출처: 토스페이먼츠)
- 멱등성은 응답 값이 같은 것만으로는 부족하고 서버 상태(DB)에도 추가 영향이 없어야 성립합니다. POST/PATCH 같은 비멱등 메서드에 재시도 안전성을 주려면 서버가 멱등키를 직접 구현해야 하며, 키는 UUID v4처럼 충분히 무작위적인 고유값을 권장합니다. (출처: 토스페이먼츠)
- 중복 요청 판별은 멱등키 단독이 아니라 멱등키 + API 키 + API 주소 + HTTP 메서드 조합으로 수행합니다. 저장소는 별도 테이블이나 Redis 같은 K-V 저장소를 쓰고, 유효기간(예: 15일)을 두어 만료 후 동일 키 재사용을 허용합니다. (출처: 토스페이먼츠)
- 멱등키 관련 에러는 형식 오류는 400, 첫 요청이 아직 처리 중일 때 동일 키 재요청은 409, 같은 멱등키에 다른 요청 본문이 오면 422로 구분해 응답하는 설계가 실무 관례입니다. (출처: 토스페이먼츠)
- 멱등성이 필요한 핵심 동기는 네트워크 오류나 타임아웃으로 응답을 못 받아 재시도하는 경우, 그리고 사용자의 중복 클릭("따닥")으로 인한 중복 결제를 막는 것입니다. (출처: 토스페이먼츠)
- 단수 명사는 도큐먼트, 복수 명사는 컬렉션으로 구분하고 계층은 `/`로 표현합니다. RFC 3986 기준 path는 대소문자를 구분하므로 소문자로 통일하는 편이 안전합니다. (출처: 스포카)
- URI에 파일 확장자(`.xml`, `.png`)를 넣지 말고 같은 URI에 대해 Content-Type 협상(헤더)으로 표현 형식을 지정합니다. 리소스와 표현(representation)은 별개라는 원칙의 실무 적용입니다. (출처: 스포카)
- CRUD로 표현하기 어려운 행위는 URI 끝에 동사를 붙인 컨트롤러 리소스로 처리하고 POST를 사용합니다(예: `/students/morgan/register`). 페이징(`pageSize=10`), 필터링(`color=red`), 응답 필드 제한(`fields=id,name`)은 query string으로 표현합니다. (출처: 스포카)
- 상태 코드는 201(생성), 202(비동기 수락 — 결과 대신 모니터링 리소스나 예상 경과 시간 안내), 409(상태 충돌), 503(과부하/점검)처럼 의미에 맞게 사용합니다. (출처: 스포카)
- LB idle timeout과 서버 keep-alive timeout이 어긋나면 간헐적 502가 납니다. ALB idle timeout 기본값 60초와 애플리케이션 서버(Gunicorn) keep-alive timeout 기본값 2초처럼 서버가 먼저 연결을 닫으면, LB는 커넥션을 유효하다고 보고 재사용하다 502를 반환합니다. (출처: SK)
- 타겟 서버의 keep-alive timeout은 반드시 LB의 connection idle timeout보다 길게 설정합니다(예: 65초 > 60초). 연결 재사용 구간마다 timeout을 정렬해야 broken pipe/reset과 502를 막을 수 있습니다. (출처: SK)

### 보안

- MSA 전환으로 백오피스가 물리적으로 분리되면서 서비스별로 흩어진 권한 관리를 중앙화하기 위해, 인증과 인가 중 인가만 전담하는 사내 권한관리 플랫폼을 도입한 사례가 있습니다 — "Centralized policy" 원칙의 실무 근거입니다. (출처: 우아한형제들)
- 권한 체크는 요청 처리 전에 유저 ID와 접근 URL을 권한 서버에 보내 접근 유효성을 판별받는 URI 기반의 단순 구조로 하고, Spring 환경에서는 Interceptor/Filter 형태의 클라이언트 라이브러리로 연동해 각 서비스의 연동 비용을 최소화했습니다. (출처: 우아한형제들)
- "심플함 우선" 설계로 기능적 한계를 인정하고 OAuth·LDAP 연동과 계정 통합은 후속 확장 과제로 남겼습니다. 중앙 권한 플랫폼이 처음부터 완전할 필요는 없다는 점진적 접근 사례입니다. (출처: 우아한형제들)
- Gateway 계층에서 인증을 일원화하는 패턴이 있습니다. 앱이 유저 식별키로 요청하면 Gateway가 인증 서버에서 디바이스와 유저 정보를 담은 인증 컨텍스트(토큰)를 발급받아 serialize해 하위 서비스로 전파하므로, 각 서비스가 유저 API를 개별 호출할 필요가 없어집니다(Netflix가 공개한 Passport 구조와 유사). (출처: 토스)
- 이 인증 컨텍스트용 유저 정보는 짧은 TTL로 캐시하고 Redis pub/sub으로 변경 이벤트 시 expire합니다 — 캐시 성능과 claim 최신성의 트레이드오프를 짧은 TTL과 무효화 이벤트로 절충한 사례입니다. (출처: 토스)
- 요청 위변조 대응으로 매 요청을 아주 짧은 유효기간의 키로 서명하고 Gateway가 서명·중복 사용·유효기간을 검증해 앱 위변조, delayed request, replay attack을 방지합니다. 의심 요청은 FDS로 넘겨 계정을 비활성화합니다. (출처: 토스)
- 대부분의 API에 종단간 암호화를 적용해 패킷 분석 허들을 높이고(앱이 요청 바디 암호화 → Gateway 복호화 시점에 인증/인가 처리), 외부사·내부 개발자 호출은 클라이언트 인증서 기반 mTLS로 처리해 X.509 SAN에서 사용자 정보를 추출해 인가와 감사에 사용합니다. (출처: 토스)
- Istio matching rule만으로도 인증/인가가 가능하지만, 코드 기반 Gateway 애플리케이션이 자유도·auditing·카나리 배포에서 유리해 애플리케이션 계층에 두었습니다 — 인프라 계층 vs 애플리케이션 계층 인가 배치의 트레이드오프입니다. (출처: 토스)
- 웹은 세션 만료 시 재인증이 자연스럽지만, 모바일 앱은 "스마트폰 = 본인 소유 기기"라는 전제 하에 자동 재발급되는 토큰(OAuth2) 방식이 UX상 낫다고 판단한 사례가 있습니다. (출처: 우아한형제들)
- 서버 저장형 토큰(opaque token)은 저장소 부하 비용이 있습니다. DB 기반 토큰 스토어에서 토큰 발급 9회, 검증 4회, 재발급 14회의 쿼리가 발생했고 동일한 client 조회 쿼리가 단일 요청에서 최대 6회 중복 실행됐으며, API 호출마다 발생하는 토큰 검증 쿼리가 가장 심각한 부하 지점이었습니다. (출처: 우아한형제들)
- AOP로 토큰 스토어의 read/write 메서드에 Redis 캐시를 주입한 결과 토큰 검증 쿼리 4회 → 0회, 발급 9회 → 5회, 재발급 14회 → 7회로 감소했습니다 — 서버 상태 저장 방식은 저장소 부하를 캐시로 상쇄해야 한다는 트레이드오프 사례입니다. (출처: 우아한형제들)
- 토큰이 만료·폐기됐는데 캐시가 유효한 구간, 즉 캐시 TTL과 토큰 유효기간의 정합 문제가 후속 과제로 남았습니다. 토큰 폐기와 캐시 무효화는 함께 설계해야 합니다. (출처: 우아한형제들)
- 서버가 복호화·재암호화를 수행하는 구조에서는 매우 짧은 순간이라도 메시지가 서버에서 평문으로 노출되는 구간이 생겨 내부 공격자 위협에 취약합니다 — 메신저에 종단간 암호화(E2EE)를 도입한 근거입니다. (출처: LINE)
- E2EE 구성은 ECDH(타원곡선 Diffie-Hellman)로 서버를 거치지 않고 양측만 아는 256비트 shared secret을 만들고, 메시지는 AES-CBC-256으로 암호화하며, 무결성은 HMAC으로 검증합니다 — 키 교환·대칭 암호화·MAC의 역할 분리를 보여주는 실무 예입니다. (출처: LINE)
- 암호화 주체가 서버에서 송신자로 이동하면서 서버는 평문 접근이 기술적으로 불가능해집니다. (출처: LINE)
- 트레이드오프도 있습니다. 양측 모두 기능을 켜야 동작하고, 초기에는 1:1 텍스트·위치 메시지에만 적용(그룹채팅 미지원)됐으며 다중 기기 사용자는 기본 제외됐습니다 — E2EE는 기능 범위·멀티 디바이스 지원과 충돌하는 비용이 있습니다. (출처: LINE)
- 전사 모든 네트워크 엔드포인트에 TLSv1.3을 적용(TLSv1.2도 병행 지원)해 전체 트래픽의 80% 이상이 TLSv1.3으로 통신하는 사례가 있습니다. TLSv1.3은 더 강한 암호화와 함께 핸드셰이크 RTT가 줄어 보안성과 성능을 동시에 개선합니다. (출처: 토스페이먼츠)
- TLS만으로는 중간자 공격을 완전히 차단할 수 없다고 보고, JWE 기반 애플리케이션 레이어 암호화를 추가 옵션으로 제공합니다 — 전송 계층 암호화와 애플리케이션 계층 암호화의 역할 분리 사례입니다. (출처: 토스페이먼츠)
- 재전송(replay) 공격 방지를 위해 JWE 헤더에 `iat`(생성 시각)와 `nonce`를 포함하고, 일정 시간을 벗어난 요청은 거부합니다. (출처: 토스페이먼츠)
- API 인증은 가맹점별 API Key를 `Authorization: Basic` 헤더로 전달하고, 웹훅은 사전 교환된 키로 만든 HMAC 서명값을 헤더에 실어 검증합니다. API 키 유출 시 피해를 줄이기 위해 비정상 반복 요청에는 429(rate limiting)를 적용합니다. (출처: 토스페이먼츠)

## CS Foundation

### 자료구조와 알고리즘

- 단건 처리 루프를 버퍼(큐 성격)에 모아 일괄 처리(배치)하면 하위 시스템 요청 수를 크게 줄일 수 있습니다. 실측으로 9,261,185건 이벤트를 233,949건 요청으로 줄여 요청 수를 97.5% 감소시켰고, 피크 CPU도 약 61%에서 약 29%로 낮췄습니다. (출처: 우아한형제들)
- 버퍼를 `Map<key, aggregated>` 형태로 두면 flush 주기 안에서 동일 키 재요청을 기존 객체에 병합(write coalescing)해 요청 수를 한 단계 더 줄일 수 있습니다. 큐 성격의 버퍼에 해시 기반 키 병합을 결합한 구조입니다. (출처: 우아한형제들)
- flush 트리거는 시간 기반(고정 주기)과 크기 기반(버퍼 임계값)으로 나뉩니다. 버퍼가 지나치게 커지면 오히려 하위 시스템에 부담을 주므로 임계값 튜닝이 필수입니다. (출처: 우아한형제들)
- 불변(immutable) 세그먼트로 구성된 색인은 건별 갱신마다 신규 세그먼트가 생겨 Disk I/O가 누적됩니다. 배치로 세그먼트 생성 빈도를 억제하는 것은 전형적인 분할 상환(amortized) I/O 최적화입니다. (출처: 우아한형제들)
- 조회 비율이 높고 원본 데이터 변경 빈도가 낮은 경우 look-aside(cache-aside) 방식이 적합합니다. Cache Miss 시 원본(DB)을 조회해 캐시에 적재하고, 이후 동일 키는 해시 기반 평균 O(1)로 응답합니다. 대상 서버는 평균 TPS 1만, 최대 2만이었고 배포 직후 조회 폭증으로 원본 DB 부하가 급증했습니다. (출처: 토스)
- 강한 일관성(strong consistency)이 필요하면 무효화(evict) "순서"가 핵심입니다. 쓰기 후 캐시 무효화는 DB commit "이후"에 수행해야 다른 스레드가 커밋 전 구버전을 다시 캐시에 적재하는 것을 막을 수 있습니다. (출처: 토스)
- commit과 evict 사이의 매우 짧은 구간(약 0.003초)에 다른 소비자가 구버전 캐시를 읽는 race condition이 발생할 수 있습니다. 무효화 이벤트 발행을 evict 처리 "이후"로 순서 보장하여 해결했으며, 자료구조 자체보다 연산 순서(happens-before)가 정합성을 좌우한 사례입니다. (출처: 토스)
- 캐시 무효화 실패 시 회로를 강제 개방(circuit force open)해 원본 직접 조회로 폴백했습니다. "잘못된 캐시 응답보다 원본 부하가 낫다"는 트레이드오프를 명시적으로 선택한 것입니다. (출처: 토스)
- 최근접 이웃 탐색에는 Exact-KNN과 ANN의 트레이드오프가 있습니다. Exact-KNN은 거리를 정확히 계산해 결과가 정확하지만 연산 비용이 크고, ANN은 recall을 합리적으로 희생해 검색 속도를 올립니다. (출처: 우아한형제들)
- HNSW(Hierarchical Navigable Small World)는 계층적 그래프를 사전(static)에 빌드하고, entry point에서 상위 레이어부터 하위 레이어로 내려가며 최근접 노드를 탐색합니다(확률 기반 스킵 리스트로 계층 구성). 그래프 인덱스 빌드 자체에 오버헤드가 있습니다. (출처: 우아한형제들)
- IVFFlat은 벡터 공간을 클러스터(nlist)로 분할해 인덱싱하고, 검색 시 nprobe 수만큼의 클러스터만 탐색해 속도를 올리되 recall 손실을 감수합니다. (출처: 우아한형제들)
- ANN의 성능 이점은 사전에 빌드된 인덱스에서 나오므로 런타임에 결정되는 동적 필터 조건과 충돌합니다. 조건으로 후보군을 먼저 줄이는 pre-filter는 그 부분집합에 대한 ANN 인덱스가 없어 Exact-KNN으로 폴백하기 쉽고, ANN 후 필터하는 post-filter는 필터 후 잔존 결과 수를 보장하지 못합니다(결과 0건 가능). (출처: 우아한형제들)
- 검색 요청은 Query step(조건에 맞는 문서 필터링), Decision step(정렬·페이징으로 최종 문서 결정), Fetch step(필드 조회·조립)으로 분해할 수 있습니다. 단계 분리는 여러 인덱스에 분산된 데이터의 통합 검색을 가능하게 합니다. (출처: 우아한형제들)
- "검색 후보군을 초기에 줄일수록 효율적"이라는 원칙에 따라, 더 많은 문서를 걸러내는 step을 앞에 배치합니다(탐색 공간 조기 축소, pruning). 인접한 동일 인덱스 step을 병합해 총 요청 단계를 4에서 3으로 축소했습니다. (출처: 우아한형제들)
- 분산 병렬 처리 효율은 Amdahl's law의 지배를 받습니다. 한 엔티티의 하위 항목이 여러 shard에 분산되면 coordinating 노드에서 이중 집계가 발생해 비효율적이며, shard 수를 4에서 8로 늘려 병렬 처리를 높인 결과 p50 11%, p99 15%가 개선되었습니다. (출처: 우아한형제들)
- 여러 필드의 함수로 정의되는 정렬 조건은 해당 필드들을 같은 인덱스(같은 위치)에 배치해야 분산 환경에서 정렬이 가능합니다. 즉 정렬 키 구성이 데이터 배치 결정을 강제합니다. (출처: 우아한형제들)

### 운영체제

- 플랫폼 스레드는 OS 커널 레벨에서 스케줄링되며 JNI를 통해 OS 스레드에 1:1로 매핑되지만, Virtual Thread는 JVM 런타임 레벨에서 스케줄링되어 스레드 생성 오버헤드가 약 10배, 메모리가 약 200배 절감됩니다. 플랫폼 스레드는 OS 최대 스레드 수에 묶이지만 경량 스레드는 힙 한도 내에서 사실상 무제한으로 만들 수 있습니다. (출처: 카카오페이)
- 스레드 수 제한이 있는 모델(플랫폼 스레드 + 톰캣 풀)은 자연스러운 back pressure로 하위 자원(DB 커넥션 풀)을 보호하지만, 경량 스레드로 유입을 무제한 수용하면 커넥션 풀 borrow timeout 예외가 발생합니다. 스레드를 늘리는 것은 병목을 하위 자원으로 옮길 뿐이므로, 전면 전환보다 I/O 집중 구간에만 선택 적용하는 편이 안전합니다. (출처: 카카오페이)
- `synchronized` 진입 시 Virtual Thread가 캐리어 스레드에 고정(pinning)되며 `-Djdk.tracePinnedThreads=short` 옵션으로 감지할 수 있습니다. MySQL 드라이버나 Jedis 같은 기존 라이브러리에서 warning이 확인되어 성능 저하가 생길 수 있습니다. (출처: 카카오페이)
- 플랫폼 스레드는 생성에 약 1ms, 컨텍스트 스위칭에 약 100µs가 들지만, JVM이 스케줄링하는 경량 스레드(Virtual Thread)는 스택 약 10KB에 생성 약 1µs, 스위칭 약 10µs로 커널 개입 없는 전환이 훨씬 저렴합니다. 스레드 1개당 스택이 약 2MB인 플랫폼 스레드는 4GB 메모리 환경에서 최대 약 4,000개가 한계입니다. (출처: 우아한형제들)
- Virtual Thread는 캐리어 스레드 위에서 실행되다가 I/O나 sleep을 만나면 park되어 힙으로 내려가고, 캐리어 스레드는 ForkJoinPool의 work stealing으로 다른 작업을 처리합니다. blocking 시 커널 스레드를 통째로 재우는 대신 유저 레벨에서 실행 흐름만 전환하는 M:N 스케줄링 사례입니다. (출처: 우아한형제들)
- 컨텍스트 스위칭 절감 효과는 워크로드 특성에 의존합니다. I/O bound 부하 테스트(힙 256MB 제한)에서는 Virtual Thread가 일반 스레드 대비 약 51% 이상 처리량이 높았고 vuser 250 초과 시 일반 스레드 서버는 장애가 발생했지만, CPU bound 작업은 스위칭 감소 이득이 없어 플랫폼 스레드가 유리합니다. (출처: 우아한형제들)
- 실무의 race condition은 중복 메시지 동시 수신, 더블 클릭·네트워크 재시도로 인한 중복 요청, 여러 작업자의 동시 조작 등으로 발현되며, read-modify-write가 원자적이지 않으면 update가 유실됩니다. (출처: 컬리)
- critical section의 경계는 공유 상태가 실제로 반영되는 시점까지 커버해야 합니다. 락 해제가 트랜잭션 커밋보다 먼저 일어나면 다음 진입자가 커밋 전 값을 읽어, 두 클라이언트가 모두 재고를 10으로 읽고 결과적으로 1개만 차감되는 정합성 문제가 생깁니다. (출처: 컬리)
- 대기 전략에는 트레이드오프가 있습니다. setnx 기반 스핀락은 락 획득까지 계속 폴링해 부하와 응답 지연을 만들고, pub/sub 방식은 락 해제 신호를 받을 때까지 블로킹 대기하는데, 이는 OS의 spinlock(바쁜 대기)과 sleep/wakeup(조건 대기) 트레이드오프와 같은 구조입니다. (출처: 컬리)
- 락에 waitTime(획득 대기 한도, 기본 5초)과 leaseTime(점유 자동 해제, 기본 3초)을 두면 락 보유자가 죽어도 영구 점유가 남지 않아, timeout 있는 lock으로 데드락·무한 대기를 방지할 수 있습니다. (출처: 컬리)
- 커넥션 풀 데드락은 "스레드 수 × 태스크당 동시 필요 커넥션 수 > pool size"일 때 발생하는 점유 대기(hold and wait)의 전형입니다. 각 스레드가 커넥션 1개를 점유한 채 두 번째 커넥션을 기다리며, 극단적으로 스레드 1개 + pool size 1 + 태스크당 커넥션 2개면 self-deadlock이 성립합니다. (출처: 우아한형제들)
- HikariCP는 기본 30초 대기 후 timeout 예외를 던져 데드락을 "영구 정지"가 아닌 "지연 + 실패"로 바꿉니다. 장애 시 `total=10, active=10, idle=0, waiting=16` 같은 자원 점유/대기 수치를 확인하는 것이 진단의 출발점입니다. (출처: 우아한형제들)
- 실제 장애(CPU 4코어, 컨슈머 스레드 16개, 풀 최대 10개)의 숨은 원인은 JPA `@GeneratedValue(AUTO)` + MySQL 조합에서 ID 채번이 별도 트랜잭션으로 추가 커넥션을 요구한 것이었습니다. 태스크당 필요 자원 수를 1개로 착각한 것이 근본 원인이므로, getConnection 디버깅으로 실제 자원 요구량을 계측해야 합니다. (출처: 우아한형제들)
- 회피 공식은 pool size = Tn × (Cm − 1) + 1 (Tn: 스레드 수, Cm: 태스크당 동시 필요 커넥션 수)이며 실제로는 여유분을 더해 설정합니다. pool size를 1로 줄이고 쿼리 1건을 실행해 timeout이 나면 한 태스크가 커넥션 2개 이상을 요구하는 코드로 판정할 수 있습니다. (출처: 우아한형제들)

### 컴퓨터 구조

- 데이터 처리 라이브러리 Polars는 벡터화 연산과 SIMD로 CPU를 최적화하는데, 그 전제로 Apache Arrow 기반 컬럼(columnar) 메모리 배치를 사용합니다. 같은 연산을 연속된 값에 일괄 적용하려면 데이터가 메모리상 칼럼 구조로 연속 배치되어야 하므로, 공간 지역성이 벡터화 효율의 조건입니다. (출처: 우아한형제들)
- 벡터화 파이프라인 중간에 Python UDF 같은 스칼라 콜백을 끼우면 "러스트 코어의 장점이나 벡터화 연산의 장점을 많이 잃어버린다"고 명시합니다. 스칼라 콜백이 SIMD 이득을 무너뜨리는 전형적 안티패턴입니다. (출처: 우아한형제들)
- 710MB parquet 읽기에서 Polars는 4초 554ms, Pandas는 33초 916ms였고, 1.8GB 처리 시 Eager API 17.84GB/9.07초에서 Lazy API 5.9GB/1.009초로 메모리와 시간이 동시에 개선되어 데이터 처리가 memory-bound임을 실측으로 보여줍니다. (출처: 우아한형제들)
- CPU 사용량(Usage)은 시간 기반(100ns 샘플링) 지표이고 활용률(Utilization)은 주파수 기반(유효 주파수÷기본 주파수) 지표라 근본이 다릅니다. 기본 2.5GHz 코어가 터보 부스트로 3.1GHz로 돌면 활용률은 3.1/2.5 = 124%로 100%를 넘을 수 있어, 시간 점유율만으로는 실제 처리량을 잡지 못합니다. (출처: 넷마블)
- 사용률 측정 단위가 논리 프로세서(Logical Processor) 기준이라 SMT/하이퍼스레딩에서 물리코어 1개가 논리코어 2개로 잡힙니다. "CPU 사용률이 낮다=여유 있다"가 항상 참이 아니며, 유효 주파수는 RDTSC 같은 하드웨어 카운터로 봐야 정확합니다. (출처: 넷마블)
- 연산량이 많은 HD 계층 압축을 전용 하드웨어 코덱에 오프로딩하면 소프트웨어 코덱 대비 압축 시간을 약 20% 수준으로 줄이고, 그동안 CPU는 병렬로 다른 작업을 진행할 수 있습니다. 특화 하드웨어로 넘겨 CPU 병렬성을 확보하는 구조입니다. (출처: LINE)
- 소프트웨어 코덱만으로는 50% 이상의 시간을 압축에만 써야 해 실시간 HD가 어렵지만, 하이브리드는 iPhone 8·HD 30FPS·10초 영상 기준 데드라인의 56% 수준에서 압축을 끝냅니다. CPU-bound 실시간 작업의 한계와 하드웨어 분담 효과를 수치로 보여줍니다. (출처: LINE)
- 멀티코어 CPU에서 각 코어는 메모리 컨트롤러를 경유해 메모리에 접근하며, 서버용 메인보드처럼 CPU 소켓이 여러 개면 소켓 간 통신 경로가 누적되어 로컬 메모리 대비 리모트 메모리 접근 비용이 커집니다. 같은 메인 메모리라도 물리적 거리(NUMA 노드)에 따라 지연이 달라진다는 점입니다. (출처: 넷마블)
- NUMA 기본 설정에서는 단일 프로세스의 스레드가 한 소켓 노드에만 몰리는 코어 쏠림이 생깁니다. 실측에서 밸런싱 없이 스레드 40개일 때 한쪽 노드만 100%(총 CPU 50%)에 대기열 26.0이었고, 밸런싱 시 양쪽 100%(총 100%)에 대기열 16.0으로, 절반의 코어를 놀리는 손실이 확인됐습니다. (출처: 넷마블)
- Node interleaving(NUMA를 UMA처럼 동작)을 켜자 코어 쏠림이 해소됐습니다. 단일 프로세스 서버 앱에서는 이 옵션을 권장하며, 메모리 배치 정책이 멀티코어 활용률을 직접 좌우함을 보여줍니다. (출처: 넷마블)

### 객체지향

- 테스트 코드를 구현 코드의 첫 사용자로 보면 좋습니다. 테스트 작성이 어렵다는 것은 설계 문제의 신호이며, 주된 관심사와 무관한 코드가 테스트에 많이 개입할수록 설계 개선이 필요하다는 뜻입니다. (출처: 카카오페이)
- SRP는 책임을 변화 관점으로 본 원칙입니다. HTTP 통신과 비즈니스 로직을 한 클래스가 함께 담당하면 API 포맷 변경 시 수정 범위가 넓어지므로 분리하는 것이 좋습니다. (출처: 카카오페이)
- 외부 라이브러리 타입을 반환값으로 노출하면 그 의존성이 모듈 전체로 전파됩니다. 반환 타입을 언어 표준 타입으로 낮추면 HTTP 클라이언트 라이브러리를 교체해도 사용하는 측에 변경이 파급되지 않습니다. (출처: 카카오페이)
- 인프라 조회와 복잡한 비즈니스 로직이 한 서비스 객체에 집중되면 테스트 Given 절 구성이 매우 어려워집니다. 비즈니스 로직만 담당하는 순수 POJO로 분리하면 외부 의존성 없이 객체 생성만으로 복잡한 케이스를 단위 테스트할 수 있습니다. (출처: 카카오페이)
- 결합도를 낮추는 수단으로 헥사고널(port-adapter) 구조를 쓸 수 있습니다. 모듈 간에는 인터페이스(port)만 노출하고 실제 구현(adapter)은 각 도메인 내부에 두며, 도메인 간에는 엔터티가 아니라 DTO·인터페이스로 데이터를 전달합니다. (출처: 우아한형제들)
- 코어 서비스 외부로 엔터티를 직접 반환하지 않는 것을 규칙으로 삼을 수 있습니다. 엔터티가 외부로 노출되면 받은 쪽에서 도메인 로직을 처리하게 되어 로직이 분산되고 서비스 간 결합도가 높아집니다. (출처: 우아한형제들)
- 의존성 방향을 코드로 강제하면 구조가 무너지지 않습니다. 서비스 모듈에서 port 인터페이스를 거쳐 도메인 모듈로 향하는 방향만 허용하고, 이 규칙을 ArchUnit 테스트로 검증해 순환·역방향 의존을 차단할 수 있습니다. (출처: 우아한형제들)
- 트레이드오프도 함께 봐야 합니다. 책임을 나누면 DTO 변환·port 정의로 코드량이 늘고 소규모 변경에도 설계 결정 비용이 커지지만, 인프라 교체 시 비즈니스 로직 무영향과 변경 영향 범위 축소라는 이득이 더 큽니다. (출처: 우아한형제들)
- 협력 객체를 외부에서 갈아끼워 격리하면 단위 테스트가 가능해집니다. 테스트 불가능한 영역(예: 미구성 인프라)이 직·간접 의존 관계를 타고 전이되면 결국 전체 코드가 테스트 불가 상태가 되므로, 그 전이를 격리해 나머지 영역의 테스트 가능성을 보존해야 합니다. (출처: 카카오페이)
- Mock 전략은 상황에 따라 선택하는 문제입니다. 실제 환경에 가장 가까운 Mock 서버, 스프링 컨텍스트 재사용을 깨뜨려 빌드가 느려지는 Bean 교체, 멀티 모듈에서 공유 가능한 test-fixtures 사이에서 실제 환경 근접도와 빌드 속도를 저울질합니다. (출처: 카카오페이)
- 테스트를 쉽게 하려고 운영 코드 설계를 훼손하지 않아야 합니다. 테스트용 Mock 설정을 운영 코드 경로에 두면 접근성은 좋아지지만 Mock이 실수로 운영 환경에 올라가는 장애 위험이 생기므로, 테스트 전용 영역으로 명시적으로 분리해야 합니다. (출처: 카카오페이)
- 템플릿 메서드 패턴에서 상위 클래스가 뼈대에서 많이 기술할수록 하위 클래스의 자유도는 줄어듭니다. 이 제약을 단점이 아니라 모순된 상태 조합을 원천 차단하는 장치로 활용할 수 있습니다. (출처: LINE)
- 상태 간 의존을 상위 클래스에서 강제하면 하위 구현이 순서를 뒤집지 못합니다. 예를 들어 선택 가능 상태가 활성 상태일 때만 성립하도록 계약을 걸면 "활성이 아닌데 선택됨" 같은 모순 상태가 애초에 만들어질 수 없습니다. (출처: LINE)
- 전략 패턴과 템플릿 메서드의 선택 기준은 명확합니다. 전략 패턴은 합성 기반으로 런타임에 알고리즘 전체를 교체할 때 쓰고, 템플릿 메서드는 상속 기반으로 알고리즘의 특정 단계만 제어할 때 씁니다. (출처: LINE)
- 공통 추상 타입에 대해 팩토리에서 일괄 호출만 하면 각 하위 구현이 자신의 단계 메서드를 오버라이딩해 다르게 동작하므로, 클라이언트에서 조건 분기를 제거할 수 있습니다. 이때 저수준이 고수준을 직접 호출하지 않는 할리우드 원칙으로 의존 흐름을 정리합니다. (출처: LINE)
- 트레이드오프도 함께 봐야 합니다. 공통 로직 중복 제거와 계약 강제라는 이득 대신, 하위 구현자가 상위 클래스 코드를 반드시 이해해야 하고 상속이 깊어지면 결합도가 커집니다. (출처: LINE)

## Architecture & Operations

### 분산 시스템

- 메시지 브로커(Kafka) 기반 이벤트 연동에서는 소비 측이 받는 순서가 발행 순서와 달라질 수 있어, "수정 → 등록"처럼 역전되면 오래된 데이터가 최신 데이터를 덮어써 결과가 비정상이 됩니다. (출처: 우아한형제들)
- 결과적 일관성은 그 자체로 안전한 것이 아니라, 역전을 감지하는 모니터링/로깅과 보정(fallback) 로직 같은 추가 검증 장치가 있어야 실제 수렴을 보장합니다. (출처: 우아한형제들)
- 순서 문제를 구조적으로 회피하려면 Zero Payload 패턴을 씁니다. 이벤트에 데이터를 싣지 않고 식별자(ID)만 전달하고 수신 측이 그 ID로 원본을 다시 조회해 저장하면, 도착 순서가 뒤바뀌어도 최종 결과가 최신값으로 수렴하고 스키마 변경에도 덜 민감합니다. 대신 수신 시마다 원본을 재조회하는 부하가 트레이드오프입니다. (출처: 우아한형제들)
- 처리에 실패한 이벤트는 별도 DeadLetter 토픽으로 격리해 재처리 경로를 둡니다. (출처: 우아한형제들)
- `synchronized` 같은 언어 수준 상호배제는 같은 프로세스 안에서만 유효하므로, 여러 인스턴스가 공유 자원을 다루는 분산 환경에서는 "동시에 하나의 writer만 자원을 변경"하도록 강제하는 분산 락이 필요합니다. (출처: 컬리)
- Redisson은 획득한 락에 `leaseTime`(자동 만료 TTL)을 둬, 락을 점유한 프로세스가 죽어도 lease 만료로 소유권을 회수합니다. 이는 락 점유자 장애로 인한 영구 점유(데드락)를 막는 fencing/lease와 같은 문제의식입니다. (출처: 컬리)
- Redis 기반 락에서 Lettuce는 `setnx`/`setex`로 계속 폴링하는 스핀락이라 대기 클라이언트가 많을수록 Redis 부하가 커지지만, Redisson은 pub/sub으로 락 해제 시 구독자에게 신호를 보내 재시도해 불필요한 폴링이 없습니다. (출처: 컬리)
- 락 해제를 트랜잭션 커밋보다 앞에 두면 안 됩니다. 락만 먼저 풀리고 다음 스레드가 변경 전 값을 읽으면(재고 10개를 두 클라이언트가 각각 10으로 읽고 차감) 정합성이 깨지므로, 별도 트랜잭션(REQUIRES_NEW)으로 처리하고 커밋 완료 후 `finally`에서 락을 해제해야 합니다. (출처: 컬리)
- 장애 판정 기준은 실패 응답(failure call)과 기준 시간 초과 응답(slow call) 두 가지입니다. `failureRateThreshold` 50%, `slowCallDurationThreshold` 60,000ms, `waitDurationInOpenState` 60,000ms, `minimumNumberOfCalls` 100 등이 기본값이며, 실제 운영에서는 실패율 10%·slow 기준 500ms·Open 대기 30초처럼 더 엄격하게 조정할 수 있습니다. (출처: 올리브영)
- 서킷브레이커가 없으면 의존 대상 장애 시 (연결 대기 timeout × 재시도 횟수)만큼 지연이 낭비되지만, 서킷이 Open이면 장애 의존성을 건너뛰고 fallback 경로(예: 대체 저장소)로 곧바로 우회합니다. fallback 메서드는 원 함수와 파라미터·반환 타입이 같아야 합니다. (출처: 올리브영)
- Resilience4j 데코레이터 기본 적용 순서는 `Retry(CircuitBreaker(RateLimiter(TimeLimiter(Bulkhead(함수)))))`입니다. Retry를 CircuitBreaker보다 바깥(먼저)에 두면 재시도 실패 횟수까지 서킷의 실패율에 합산되므로 임계값 설정에 주의해야 합니다. (출처: 올리브영)
- Resilience4j는 Hystrix가 maintenance 모드로 전환되며 그 대체로 쓰이는 경량 라이브러리로, CLOSED/OPEN/HALF_OPEN 외에 항상 허용하는 DISABLED와 항상 거부하는 FORCED_OPEN 특수 상태가 있고 실패율은 슬라이딩 윈도우(COUNT_BASED/TIME_BASED)로 측정합니다. (출처: 우아한형제들)
- fallback은 상태와 무관하게 CLOSED에서도 메서드가 실패하면 실행되므로 fallback 실행 여부만으로 서킷 Open을 판단할 수 없고, fallback이 실행되면 원래 예외가 상위로 전파되지 않아 기존 예외 처리 흐름을 함께 조정해야 합니다. Open 전용 예외인 `CallNotPermittedException`은 별도 분기 처리할 수 있습니다. (출처: 우아한형제들)

### DevOps & Cloud

- 제품이 성장하면 기능 개발보다 모니터링 환경 구축을 먼저 해야 합니다. 트래픽이 커진 뒤 지표가 없으면 장애 원인을 추론할 수 없기 때문입니다. (출처: 토스)
- 수집 대상을 컴포넌트 지표와 서비스 지표로 나눌 수 있습니다. 컴포넌트 지표는 Redis 커맨드 지연, DB Insert QPS, Kafka Consumer 처리율, 게이트웨이 요청량 변화 등이고, 서비스 지표는 PV, UV, 리텐션 같은 비즈니스 지표입니다. (출처: 토스)
- Metric은 단순 증상 확인을 넘어 원인과 결과를 예상하고 해결책을 제시하는 데 쓰입니다. 즉 상태 감시뿐 아니라 근본 원인 추론에 활용합니다. (출처: 토스)
- 모니터링은 일회성이 아니라 사이클입니다. 모니터링, 문제 파악, 해결책 적용, 카나리 배포 후 이전 버전과의 성능 비교 재모니터링으로 이어지며, 로그는 개선 전후 효과를 확인하는 근거가 됩니다. (출처: 토스)
- CI/CD 도입 동기는 수동 배포의 Human Error였습니다. 여러 산출물을 직접 만들어 배포하면 실수가 나기 쉽고, 특히 모바일 앱은 잘못 배포하면 재배포가 어려워 자동화 필요성이 큽니다. (출처: 우아한형제들)
- CI 도구는 무료 사용, 커스터마이징 자유도, 플러그인 생태계, 레퍼런스 풍부함, Remote Access API 제공 여부를 비교해 선택할 수 있습니다. (출처: 우아한형제들)
- 빌드 서버 자체를 Dockerfile로 코드화(베이스 이미지에 JDK, 빌드 도구, SDK를 포함)하면 설정과 설치 과정을 재현 가능하게 만들고 운영 리소스를 절감할 수 있습니다. (출처: 우아한형제들)
- 빌드와 배포 결과를 메신저 채널로 자동 통보하면 팀 전체의 가시성을 확보할 수 있습니다. 통과 시 테스터에게 배포를 요청하고 실패 시 즉시 피드백합니다. (출처: 우아한형제들)
- 변경 규모에 따라 배포 전략을 분기할 수 있습니다. 기존 API 연동에 영향이 없는 Patch/Minor 변경은 Rolling Update로 자동 무중단 배포하고, API 입출력이 바뀌는 Major(breaking change)는 Blue/Green으로 전환하되 수동 승인 후 배포합니다. (출처: 우아한형제들)
- Blue/Green 전환은 신규 버전으로 모든 API 호출을 옮긴 뒤 기존 버전을 삭제하는 흐름입니다. Major 배포를 수동으로 두는 이유는 하위 호환성이 깨질 수 있기 때문입니다. (출처: 우아한형제들)
- breaking change 여부는 oasdiff 같은 도구로 현행 서빙 API와 신규 API의 스펙 차이를 비교해 배포 파이프라인에서 자동 감지할 수 있습니다. (출처: 우아한형제들)
- 배포 명세를 Git으로 선언적으로 관리(GitOps)하면 이전 상태로 되돌리기 쉬워 롤백이 수월합니다. 모델과 산출물을 이미지에 포함(bake-in)해 서빙 로직과 버전을 일치시키면 문제 발생 시 빠르게 복구할 수 있습니다. (출처: 우아한형제들)
- Graceful shutdown은 만능이 아닙니다. 작업 소요 시간이 수십 분 단위로 길면 진행 중 작업 완료를 기다리는 동안 배포가 블로킹되어 배포 자체가 30분 이상 걸릴 수 있어 병목이 됩니다. (출처: 우아한형제들)
- 대안으로 프로세스를 즉시 종료하되 작업 상태를 외부 DB에 영속화하고, 중단된 작업을 외부에서 감지해 재처리하는 설계를 택할 수 있습니다. 이 방식은 작업의 재처리 가능성(idempotency)과 외부 복구 메커니즘이 전제되어야 합니다. (출처: 우아한형제들)
- Heartbeat 패턴에서는 워커가 주기적으로(예: 1분마다) DB에 생존 신호를 갱신하고, 일정 시간(예: 2분) 이상 갱신이 없으면 스케줄러가 비정상으로 간주해 작업을 대기 상태로 되돌려 다른 인스턴스가 이어받게 합니다. (출처: 우아한형제들)
- 복구 스케줄러가 여러 인스턴스에서 동시에 실행되지 않도록 분산 락(ShedLock류)으로 단일 인스턴스 실행을 보장합니다. 배포 무중단성은 graceful shutdown 단독으로 보장하기 어렵고 작업 상태의 영속성과 재처리 가능성이 함께 설계되어야 합니다. (출처: 우아한형제들)
- 쿠버네티스 Pod에 기본값(CPU 2 core, Memory 4GiB)으로 할당했지만 실측 결과 0.5 core 미만을 쓰는 Pod이 80%에 달할 만큼 과할당 상태였습니다. 기본값이 아니라 실사용량을 기준으로 리소스를 재설계하는 것이 자원 절감의 출발점입니다. (출처: 카카오페이)
- 수평 확장(HPA, Pod 수 증가)과 수직 확장(VPA, Pod당 리소스 증가)은 함께 사용할 수 없습니다. 이미 HPA를 쓰고 있다면 VPA를 도입할 수 없어 별도의 추천 로직이 필요합니다. (출처: 카카오페이)
- 리소스 추천값은 단순 평균이 아니라 95th percentile에 15% 안전 마진과 최근 2주 데이터를 반영해 산정했습니다. 수집 기간이 짧을수록 신뢰도를 보정해 OOM을 방지하는 통계적 버퍼링 방식입니다. (출처: 카카오페이)
- 기동(warm-up) 시 request 최솟값만 적용하면 순간 리소스 부족으로 Readiness Probe가 실패할 수 있습니다. limit을 상향(Burstable QoS)해 기동 시 여유를 주고 정상 운영 시에는 낮은 request를 유지하는 방식으로 해결했습니다. (출처: 카카오페이)

## 참고한 기술블로그

- 토스 — 가장 많은 트래픽을 받는 토스 서비스의 서버 관리 전략: https://toss.tech/article/monitoring-traffic
- 카카오페이 — 실무에서 적용하는 테스트 코드 작성 방법과 노하우 Part 1: 효율적인 Mock Test: https://tech.kakaopay.com/post/mock-test-code/
- 카카오페이 — 실무에서 적용하는 테스트 코드 작성 방법과 노하우 Part 2: 테스트 코드로부터 피드백 받기: https://tech.kakaopay.com/post/mock-test-code-part-2/
- 컬리 — 풀필먼트 입고 서비스팀에서 분산락을 사용하는 방법 - Spring Redisson: https://helloworld.kurly.com/blog/distributed-redisson-lock/
- 우아한형제들 — 전체 서비스를 관통하는 도메인 모듈을 안전하게 분리하기: https://techblog.woowahan.com/22151/
- 올리브영 — 주문결제 트랜잭션 분리를 통한 성능 향상 (올리브영 결제 이야기 Part-3): https://oliveyoung.tech/2022-12-13/oliveyoung-transaction-orderstock/
- 화해 — 캐시 스탬피드를 대응하는 성능 향상 전략, PER 알고리즘 구현: https://blog.hwahae.co.kr/all/tech/14003
- 우아한형제들 — 우리 팀은 카프카를 어떻게 사용하고 있을까: https://techblog.woowahan.com/17386/
- 우아한형제들 — 실시간 반응형 추천 개발 일지 2부: 벡터 검색, 그리고 숨겨진 요구사항과 기술 도입 의사 결정을 다루는 방법: https://techblog.woowahan.com/21027/
- 올리브영 — 고성능 캐시 아키텍처 설계 - 로컬 캐시와 Redis로 대규모 증정 행사 관리 최적화: https://oliveyoung.tech/2024-12-10/present-promotion-multi-layer-cache/
- 토스 — 캐시 문제 해결 가이드 - DB 과부하 방지 실전 팁: https://toss.tech/article/cache-traffic-tip
- 우아한형제들 — 개발자 의식의 흐름대로 적용해보는 서킷브레이커: https://techblog.woowahan.com/15694/
- 우아한형제들 — 이벤트 기반 아키텍처 도입 사례(전시 영역): https://techblog.woowahan.com/13101/
- 토스 — 캐시를 적용하기까지의 험난한 길 (TPS 1만 안정적으로 서비스하기): https://toss.tech/article/34481
- 우아한형제들 — 검색을 위한 데이터 다루기: https://techblog.woowahan.com/2718/
- 우아한형제들 — 라이더스 개발팀 모바일에서 CI/CD 도입: https://techblog.woowahan.com/2579/
- 우아한형제들 — 실시간 인덱싱을 위한 Elasticsearch 구조를 찾아서: https://techblog.woowahan.com/7425/
- 우아한형제들 — 도움이 될수도 있는 JVM memory leak 이야기: https://techblog.woowahan.com/2628/
- 컬리 — 분산 시스템 환경에서 Kafka Consumer 오프셋 이동하기: https://helloworld.kurly.com/blog/2024-spring-kafka-consumer-offset-seeking/
- 올리브영 — 개발자가 알면 좋은 Redis 꿀팁 모음: https://oliveyoung.tech/2025-07-23/redis-tips-for-developer/
- 우아한형제들 — 빌링 시스템 장애, 이러지 말란 Maria~: https://techblog.woowahan.com/2517/
- 우아한형제들 — 장시간 비동기 작업, Kafka 대신 RDB 기반 Task Queue로 해결하기: https://techblog.woowahan.com/23625/
- 카카오페이 — 지연이체 서비스 개발기: 은행 점검 시간 끝나면 송금해 드릴게요! (feat. 발표 후기): https://tech.kakaopay.com/post/ifkakao2024-delayed-transfer/
- 우아한형제들 — 제목은 안정적인 AI 서빙 시스템으로 하겠습니다. 근데 이제 자동화를 곁들인…: https://techblog.woowahan.com/19548/
- 넷마블 — 단일 프로세스에서 NUMA가 야기한 성능 저하: https://netmarble.engineering/single-process-programming-numa-effect/
- 당근 — 확장성 있는 TCP 통신 시스템 구축하기 — Netty를 활용한 TCP Gateway 구축기: https://medium.com/daangn/확장성-있는-tcp-통신-시스템-구축하기-981c230cdc77
- 컬리 — 데이터가 있었는데요, 아니 없어요: https://helloworld.kurly.com/blog/commit-mvcc-set-autocommit/
- 토스페이먼츠 — 멱등성이 뭔가요?: https://docs.tosspayments.com/blog/what-is-idempotency
- 카카오페이 — 환경미화 프로젝트(부제: 카카오페이 k8s에서 낭비되는 자원을 절약해 보자!): https://tech.kakaopay.com/post/eco-ami/
- 토스 — 토스는 Gateway 이렇게 씁니다: https://toss.tech/article/slash23-server
- 토스페이먼츠 — 토스페이먼츠의 Open API 생태계: https://toss.tech/article/payments-legacy-4
- 카카오페이 — [Project Loom] Virtual Thread에 봄(Spring)은 왔는가: https://tech.kakaopay.com/post/ro-spring-virtual-thread/
- 컬리 — 99%가 모른다는 DB Connection 누수 문제: https://helloworld.kurly.com/blog/connection-leak/
- 우아한형제들 — aop를 이용한 oauth2 캐시 적용하기: https://techblog.woowahan.com/2617/
- 토스페이먼츠 — API 응답을 받고 나면 어떻게 해요?: https://www.tosspayments.com/blog/articles/dev-5
- 올리브영 — Circuitbreaker를 사용한 장애 전파 방지: https://oliveyoung.tech/2023-08-31/circuitbreaker-inventory-squad/
- 넷마블 — CPU 이용률의 두 가지 얼굴 – CPU 코어 사용량(Usage)과 활용률(Utilization): https://netmarble.engineering/cpu-core-usage-and-utilization/
- 우아한형제들 — HikariCP Dead lock에서 벗어나기 (실전편): https://techblog.woowahan.com/2663/
- 우아한형제들 — HikariCP Dead lock에서 벗어나기 (이론편): https://techblog.woowahan.com/2664/
- 우아한형제들 — IllegalArgumentException은 400 Bad Request인가?: https://techblog.woowahan.com/21686/
- 우아한형제들 — Java의 미래, Virtual Thread: https://techblog.woowahan.com/15398/
- 카카오페이 — JPA Transactional 잘 알고 쓰고 계신가요?: https://tech.kakaopay.com/post/jpa-transactional-bri/
- 하이퍼커넥트 — JVM + Container 환경에서 수상한 Memory 사용량 증가 현상 분석하기: https://hyperconnect.github.io/2022/07/19/suspicious-jvm-memory-in-container.html
- 올리브영 — Kafka 메시지 중복 및 유실 케이스별 해결 방법: https://oliveyoung.tech/2024-10-16/oliveyoung-scm-oms-kafka/
- 카카오페이 — Kotlin 환경에서 로그를 기록할 때 불필요한 문자열 연산을 방지하는 방법: https://tech.kakaopay.com/post/efficient-logging-with-kotlin/
- 카카오페이 — Kotlin으로 Spring AOP 극복하기!: https://tech.kakaopay.com/post/overcome-spring-aop-with-kotlin/
- 카카오페이 — MSA 환경에서 네트워크 예외를 잘 다루는 방법: https://tech.kakaopay.com/post/msa-transaction/
- 당근 — MySQL Gap Lock 다시보기: https://medium.com/daangn/mysql-gap-lock-%EB%8B%A4%EC%8B%9C%EB%B3%B4%EA%B8%B0-7f47ea3f68bc
- 우아한형제들 — OSORI 권한관리 플랫폼: https://techblog.woowahan.com/2519/
- 우아한형제들 — Polars로 데이터 처리를 더 빠르고 가볍게 with 실무 적용기: https://techblog.woowahan.com/18632/
- 우아한형제들 — PostgreSQL Vacuum에 대한 거의 모든 것: https://techblog.woowahan.com/9478/
- 에스코어 — Redis 메모리, Eviction 정책으로 알차게 쓰는 법: https://osslab.s-core.co.kr/27d035f5-1294-80c1-bd56-e8e87c01948e
- 우아한형제들 — Redis New Connection 증가 이슈 돌아보기: https://techblog.woowahan.com/23121/
- 스포카 — RESTful API를 설계하기 위한 디자인 팁: https://spoqa.github.io/2013/06/11/more-restful-interface.html
- 우아한형제들 — Spring 웹 애플리케이션에서 사용하지 않는 API를 찾아보자: https://techblog.woowahan.com/2610/
- 카카오페이 — Spring Bean Injection 이야기(feat. 모두가 다 알고 있는 스프링빈, 정말 다 알고 있는가?): https://tech.kakaopay.com/post/martin-dev-honey-tip-2/
- 하이퍼커넥트 — Spring Transactional Rollback Deep Dive: https://hyperconnect.github.io/2025/02/10/spring-transactional-rollback.html
- 하이퍼커넥트 — Spring WebClient에서 새어나가는 메모리 잡기: https://hyperconnect.github.io/2022/10/07/troubleshoot-webclient-oom.html
- LINE — 템플릿 메서드 패턴으로 모순 없는 상태 보장하기: https://engineering.linecorp.com/ko/blog/templete-method-pattern/
- LINE — 하드웨어 비디오 코덱과 소프트웨어 비디오 코덱의 하이브리드!: https://engineering.linecorp.com/ko/blog/develop-hardware-software-hybrid-codec
- LINE — 더 안전한 대화를 위한 Letter Sealing: https://engineering.linecorp.com/ko/blog/new-generation-of-safe-messaging-letter-sealing
- LINE — LINE에서 Kafka를 사용하는 방법 - 1편: https://engineering.linecorp.com/ko/blog/how-to-use-kafka-in-line-1/
- LINE — messaging-hub 트러블 슈팅: https://engineering.linecorp.com/ko/blog/messaing-hub-troubleshooting
- SK — AWS ALB로 부터 반환되는 502 bad gateway 에러 트러블슈팅: https://devocean.sk.com/blog/techBoardDetail.do?ID=165428&boardType=techBlog
